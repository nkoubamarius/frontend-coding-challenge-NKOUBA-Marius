import React from "react";
import { RepositoryContainer } from "./RepositoryElements";

const Repository = () => {
  return <RepositoryContainer>ok</RepositoryContainer>;
};

export default Repository;
